*Getting Started*

Install Rails at the command prompt if you haven't yet:

$ gem install rails
At the command prompt, create a new Rails application:

Change directory to nuli and start the web server:

$ cd myapp
$ rails server
Run with --help or -h for options.

Using a browser, go to http://localhost:3000 and you'll see the page.
